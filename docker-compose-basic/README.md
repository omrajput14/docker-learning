# Basic Docker Compose Example

This example demonstrates how to orchestrate multi-container Docker applications using `docker-compose`.

## How to run

1. Start all services:
   ```bash
   docker-compose up -d
   ```
2. Open your browser and navigate to `http://localhost:8082`.
3. Stop services:
   ```bash
   docker-compose down
   ```
